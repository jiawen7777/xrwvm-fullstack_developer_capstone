apiVersion: apps/v1
kind: Deployment
metadata:
  name: node-api-deployment
  namespace: dealership
spec:
  replicas: 1
  selector:
    matchLabels:
      app: node-api
  template:
    metadata:
      labels:
        app: node-api
    spec:
      containers:
        - name: node-api
          image: nodeapp:1.0
          ports:
            - containerPort: 3030